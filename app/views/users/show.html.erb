<ul>
  <% @user.comments.each do |comment| %>
    <li>
      Comment on <%= link_to Post.find(comment.post_id).title, post_path(comment.post_id) %>: <%= comment.content %>
    </li>
  <% end %>
</ul>
